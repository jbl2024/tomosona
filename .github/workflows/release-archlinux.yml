name: Release (Arch Linux)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build Arch package (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runs-on: ubuntu-24.04
            container-image: docker.io/archlinux/archlinux:base-devel
          - arch: aarch64
            runs-on: ubuntu-24.04-arm
            # Arch's official container image is amd64-only; use an Arch Linux ARM
            # build environment on the arm64 runner for the aarch64 package.
            container-image: docker.io/bamboocz/archlinuxarm:latest

    runs-on: ${{ matrix.runs-on }}
    container:
      image: ${{ matrix.container-image }}
      options: --user 0

    steps:
      - uses: actions/checkout@v7

      - name: Resolve release version
        id: release_version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.0-dev"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Install Arch build dependencies
        run: |
          pacman-key --init
          case "${{ matrix.arch }}" in
            x86_64)
              pacman-key --populate archlinux
              ;;
            aarch64)
              pacman-key --populate archlinuxarm
              ;;
            *)
              echo "Unsupported Arch package target: ${{ matrix.arch }}"
              exit 1
              ;;
          esac
          pacman -Syu --noconfirm --needed
          pacman -S --noconfirm --needed \
            base-devel \
            git \
            nodejs \
            npm \
            rust \
            cargo \
            webkit2gtk-4.1 \
            gtk3 \
            libayatana-appindicator \
            librsvg \
            xdg-utils \
            patchelf

      - name: Prepare package build user
        run: |
          useradd -m builder
          chown -R builder:builder "$GITHUB_WORKSPACE"

      - name: Build Arch package
        shell: bash
        run: |
          set -euo pipefail

          version="${{ steps.release_version.outputs.version }}"
          workdir="$GITHUB_WORKSPACE/.archlinux-${{ matrix.arch }}"
          pkgdir="$workdir/packaging"
          pkgdest="$workdir/pkgdest"
          artifactdir="$GITHUB_WORKSPACE/release-artifacts-${{ matrix.arch }}"

          rm -rf "$workdir" "$artifactdir"
          mkdir -p "$pkgdir" "$pkgdest" "$artifactdir"

          cp -R packaging/archlinux/. "$pkgdir/"
          sed -i "s/__PKGVER__/${version}/g" "$pkgdir/PKGBUILD"

          chown -R builder:builder "$workdir"

          su builder -c "cd '$pkgdir' && PKGDEST='$pkgdest' AWS_LC_SYS_NO_JITTER_ENTROPY=1 makepkg --noconfirm --cleanbuild --skipinteg"

          echo "Package output directory:"
          ls -al "$pkgdest"

          shopt -s nullglob
          package_files=(
            "$pkgdest"/*.pkg.tar
            "$pkgdest"/*.pkg.tar.zst
            "$pkgdest"/*.pkg.tar.xz
            "$pkgdest"/*.pkg.tar.gz
            "$pkgdest"/*.pkg.tar.bz2
            "$pkgdest"/*.pkg.tar.lz4
            "$pkgdest"/*.pkg.tar.lz
            "$pkgdest"/*.pkg.tar.lzo
            "$pkgdest"/*.pkg.tar.lrz
            "$pkgdest"/*.pkg.tar.Z
          )
          if [ "${#package_files[@]}" -eq 0 ]; then
            echo "No Arch package was produced in $pkgdest"
            exit 1
          fi

          mv "${package_files[@]}" "$artifactdir/"

          echo "Release artifact directory:"
          ls -al "$artifactdir"

      - name: Upload Arch package artifact
        uses: actions/upload-artifact@v7
        with:
          name: archlinux-${{ matrix.arch }}
          path: release-artifacts-${{ matrix.arch }}/*
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Show files
        run: find artifacts -type f

      - name: Create Release + Upload Assets
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: false
          files: artifacts/**/*
